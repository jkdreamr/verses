import { Dashboard } from "@/components/Dashboard";

export const metadata = { title: "Your songs — Verses" };
export const dynamic = "force-dynamic";

export default function AppPage() {
  return <Dashboard />;
}
